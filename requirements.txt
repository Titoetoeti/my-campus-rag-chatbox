# ============================================================
# RAG Chatbox - Requirements
# Cài đặt: pip install -r requirements.txt
# ============================================================

# --- Giao diện web ---
streamlit>=1.45.0

# --- Biến môi trường (.env) ---
python-dotenv>=1.0.0

# --- Đọc PDF ---
pdfplumber>=0.11.0

# --- LangChain core + community ---
langchain-core>=0.3.0
langchain-community>=0.3.0

# --- LLM: Groq (Llama 3.3 70B) ---
langchain-groq>=0.3.0

# --- Vector store ---
faiss-cpu>=1.9.0

# --- Embedding model (multilingual-e5-small) ---
sentence-transformers>=3.0.0

# --- Backend cho HuggingFace embeddings ---
torch>=2.0.0
transformers>=4.40.0
# Giai đoạn 2 — Hybrid Retrieval: Lexical Search (BM25)
rank_bm25>=0.2.2
 
# Giai đoạn 3 — Reranking: Cross-Encoder BGE-reranker-v2-m3 (chạy local, CPU)
sentence-transformers>=3.0.0
 
# (Khuyến nghị, KHÔNG bắt buộc) Tăng tốc tải model reranker (~2.27GB) lần đầu
# từ HuggingFace — giảm thời gian khởi tạo AI đáng kể trên mạng tốt. Nếu
# không cài, code vẫn chạy bình thường, chỉ tải bằng phương thức mặc định
# (chậm hơn).
hf_transfer>=0.1.6
 
